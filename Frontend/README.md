# Frontend

Flutter frontend skeleton for BepTroLy.

Structure:

- lib/core: constants, theme, utils, services (`api_service.dart`)
- lib/data: models, repositories, datasources
- lib/presentation: placeholder pages
- lib/routes: `app_router.dart`

Run:

```bash
cd Frontend
flutter pub get
flutter run
```
